# Runtime sprite exports

Drop exported PNGs from `art/authoring/` here using the names defined in
`public/src/art_assets.js`. The browser loads them automatically on the next
refresh and falls back only when a file is absent.

Current external-authoring targets:

- `dungeon_void_raider.png`
- `dungeon_dawn_guard.png`
